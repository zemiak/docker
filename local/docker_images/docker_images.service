[Unit]
Description=Hosted docker services, see /opt/docker_images/
After=docker.service

[Service]
ExecStart=sleep 10s && /usr/bin/jjs /opt/docker_images/docker_images.js -- start

[Install]
WantedBy=multi-user.target
